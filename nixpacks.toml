[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd packages/backend && npm install --legacy-peer-deps"]

[phases.build]
cmds = ["cd packages/backend && npm run build"]

[start]
cmd = "cd packages/backend && npm run start:prod"
